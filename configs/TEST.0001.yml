lac_id: "LAC.0001"
test_id: "TEST.0001"

target:
  base_url: "https://api.dev.exemplo.local"   # ajusta para o teu endpoint

load:
  duration_s: 300          # duração em segundos
  rps: 200                 # throughput alvo em RPS (Requests Per Second)
  concurrency: 50          # utilizadores virtuais

labels:
  environment: "dev"
  system: "payments"
  notes: "baseline"
